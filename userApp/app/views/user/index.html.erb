
<h1>Listing users</h1>
 

<%= link_to 'New user', new_user_path %>

<table>
  <tr>
    <th>Family name</th>
    <th>Name</th>
  </tr>
 
  <% @user.each do |u| %>
    <tr>
      <td><%= u.family_name %></td>
      <td><%= u.name %></td>
      <td><%= link_to 'Show', user_path(u) %></td>
    </tr>
  <% end %>
</table>